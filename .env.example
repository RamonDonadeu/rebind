# =============================================================================
# ReBind — copy to .env for local development
# =============================================================================

# --- App ---
NODE_ENV=development

# Docker dev: file watching on Windows bind mounts needs polling (default true in docker-compose.yml)
# CHOKIDAR_USEPOLLING=true
# WATCHPACK_POLLING=true
WEB_PORT=3000
API_PORT=4000
NEXT_PUBLIC_API_URL=http://localhost:4000
API_INTERNAL_URL=http://api:4000
CORS_ORIGIN=http://localhost:3000

# --- PostgreSQL ---
POSTGRES_USER=rebind
POSTGRES_PASSWORD=rebind
POSTGRES_DB=rebind
# Host Prisma / local tools — use localhost. Docker Compose overrides to @postgres for api.
DATABASE_URL=postgresql://rebind:rebind@localhost:5432/rebind

# --- Redis ---
REDIS_URL=redis://redis:6379

# --- Auth (change in production) ---
JWT_SECRET=dev-jwt-secret-change-me
JWT_REFRESH_SECRET=dev-refresh-secret-change-me
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# --- Dev only: collector limits without Stripe (50 binders, 48 pages). Never true in production. ---
DEV_FULL_PLAN=true

# --- Stripe (Phase 4 — optional) ---
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# STRIPE_PRICE_COLLECTOR_MONTHLY=
# STRIPE_PRICE_COLLECTOR_YEARLY=

# --- TCGdex ---
TCGDEX_LANG=en
TCGDEX_BASE_URL=https://api.tcgdex.net/v2

# --- Logging (structured JSON → stdout; collected by docker/logging stack) ---
LOG_LEVEL=debug
LOG_SERVICE_NAME=rebind-api
LOG_PRETTY=false
